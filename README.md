# ml-image-captioning-cnn-rnn
End-to-end image captioning pipeline on MS COCO using a CNN encoder (visual features) and RNN decoder (caption generation), with auxiliary image classification supervision.
